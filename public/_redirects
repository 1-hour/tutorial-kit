# _redirects — Cloudflare Workers static assets redirects
#
# Docs: https://developers.cloudflare.com/workers/static-assets/redirects/
#
# Root URL must NOT be a standalone document — it's a locale gateway.
# Send it to the default locale home via a real 302 (temporary, because
# we may later want language negotiation based on Accept-Language).
#
# Format: <source> <destination> [status_code, default 302]
/  /en/  302
